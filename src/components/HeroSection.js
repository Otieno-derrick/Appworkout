import React from 'react';
import { Box, Stack, Typography} from '@mui/material';

const HeroSection = () => {
  return (
    <Box>
      <Typography>
        replace
      </Typography>
    </Box>
  )
}

export default HeroSection